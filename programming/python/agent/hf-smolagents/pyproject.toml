[project]
name = "hf-smolagents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "smolagents>=1.5.0",
    "transformers>=4.48.1",
]
